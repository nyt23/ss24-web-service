name: jest-tests
on: [push]
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      run:
        - uses: actions/checkout@v4
        - name: Install dependencies
          run: npm install
        - name: Run jest tests
          run: npm test
